# Proyecto de Supabase donde viven las tablas `rsvps`, `songs` y `clients`.
# Se leen desde el navegador, así que solo van aquí claves públicas (anon).
NEXT_PUBLIC_SUPABASE_URL="https://TU-PROYECTO.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="TU_ANON_KEY"
